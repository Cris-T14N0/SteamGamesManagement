name: FTP Deploy

on:
  push:
    branches:
      - main   # change if you deploy from another branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.antrob.eu
          username: u506280443.crijul
          password: "&8Mj8F7XVgHODXly"
          local-dir: ./
          server-dir: /
